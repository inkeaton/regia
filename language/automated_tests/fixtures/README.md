# Test fixtures directory
# Place .rgs test files here, organised by category:
#
#   fixtures/
#   ├── valid/           ← files that should compile cleanly
#   ├── syntax_errors/   ← files with intentional syntax errors
#   ├── semantic_errors/ ← files with undeclared refs, duplicates, etc.
#   └── integration/     ← full scenarios for snapshot testing
